# P8 — Link pass for wave 1 (PLAN.md §15.8, §15.9)

Sonnet, wave 1 lane 2, phase 6 and last of the sequential
`sonnet-wave1-lane2.md` run. Branch `phase/p8` off `main` at `f2119cf`
(P7 + its pre-handoff fix), after P3–P7 all merged.

## Built

No feature code — cross-cutting only, per this phase's own rule.

- **Nav reachability audit.** Walked every surface P3–P7 added:
  `/inbox/quick-replies` (linked from `/inbox`), `/contacts/campos`
  (linked from `/contacts`), the email section on `/settings` (already on
  the page the sidebar's existing "Settings" item opens), the notifications
  bell (already in the header since P1), `/pipeline/etapas`'s new
  stale-threshold field (same existing link from `/pipeline`), `/r/[token]`
  (linked as "Ver recibo" from a document's payment row), and the "Hoy"
  panel (already the first thing on `/dashboard`). Every one was already
  reachable — no dead-ends found, so `src/components/app-nav.tsx` and the
  layout's nav groups needed no change. Nothing in the dashboard onboarding
  checklist represents a wave-1 milestone either (quick replies, custom
  fields, receipts and the coach panel are enhancements, not first-run
  setup steps), so that file is unchanged too.
- **`docs/HANDOFF.md`** — new Part 3: the six migrations (`0029`–`0034`),
  the two optional env vars (`WEB_PUSH_*`, `EMAIL_DEFAULT_DOMAIN`) with
  their setup commands, and the two human checks (push delivered to an
  installed Android PWA, a tenant's Resend domain verifying end to end).
- **`docs/SMOKE_TEST.md`** — new §9, one checklist row per P1–P7 exit
  criterion that a click-through can verify.
- **`KNOWN-ISSUES.md`** (new) — the still-open cross-phase items promoted
  from `docs/log/p1.md` through `p7.md`'s own "Known issues" sections; a
  fixed-and-superseded item (P1's `quote_accepted` emitter, since built by
  P6) was left out rather than promoted.

## Decisions and deviations

1. **No nav/menu code changed.** The phase's Owns column lists
   `src/components/app-nav.tsx` as available to touch, not required to be
   touched — every new surface audited was already linked from somewhere a
   user would naturally land, so adding sidebar entries for sub-features
   already reachable in two clicks would be nav clutter, not a fix.
2. **The dashboard onboarding checklist is unchanged**, for the same
   reason: none of P3–P7's additions are a first-run setup step.

## Known issues

See `KNOWN-ISSUES.md` — this phase's own contribution to it is the file
itself, not a new item.

## Verification

`npm run lint`, `npm run typecheck`, `npm run build` green.
`npm test` green at **922 passing, 0 skipped** (MySQL installed and
migrated locally) — unchanged from P7, since this phase touched no code.
